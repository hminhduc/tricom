= javascript_include_tag 'ekis/index'
= provide(:title, (t 'title.eki'))
%ol.breadcrumb
  %li= link_to (t 'title.menu'), main_path
  %li.active= (t 'title.ekimaster')

%table#eki.table.table-striped.table-bordered.table-hover{cellspacing:0, width: '100%'}
  %thead
    %tr
      %th= (t 'title.ekicode')
      %th= (t 'title.ekimei')
      %th= (t 'title.ekimeikana')
      %th
  %tbody
    = render @ekis
= render 'new'
= render 'edit'