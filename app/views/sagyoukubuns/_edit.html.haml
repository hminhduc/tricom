.modal#sagyoukubun-edit-modal{tabindex:-1, role: :dialog, 'aria-labelledby'=>"myModalLabel", 'aria-hidden'=>"true"}
  .modal-dialog.modal-sm
    .modal-content
      .modal-header
        %button.close{type: :button, 'data-dismiss'=>"modal", 'aria-hidden'=>"true"} &times;
        %h4.modal-title#myModalLabel 作業区分更新
      .modal-body
        = simple_form_for :sagyoukubun, url: sagyoukubun_path(id: 0), method: :patch, :remote => true, html: { id: 'edit_sagyoukubun', role: :form, 'data-model' => 'sagyoukubun'} do |f|
          = f.input :作業区分 do
            = f.input_field :作業区分, readonly: true, class: 'form-control'
            .span.help-block
          = f.input :作業区分名称 do
            = f.input_field :作業区分名称, class: 'form-control'
            .span.help-block
          = f.button :submit, class: 'btn btn-primary'