= javascript_include_tag 'bashokubunmsts/index'
= provide(:title, (t 'title.bashokubunmst'))
%ol.breadcrumb
  %li= link_to (t 'title.menu'), main_path
  %li.active= (t 'title.bashokubunmst')

%table#bashokubunmst_table.table.table-striped.table-bordered.table-hover{cellspacing: 0, width: '100%' }
  %thead
    %tr
      %th= (t 'title.bashokubuncode')
      %th= (t 'title.bashokubunmei')
      %th

  %tbody
    = render @bashokubunmsts

= render 'new'
= render 'edit'