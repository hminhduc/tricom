= javascript_include_tag 'tsushinseigyous/index'
= provide(:title, (t 'title.tsushinseigyou'))
%ol.breadcrumb
  %li= link_to (t 'title.menu'), main_path
  %li.active 通信制御マスタ

%table#tsushinseigyou.table.table-striped.table-bordered.table-hover{cellspacing: 0, width: '100%' }
  %thead
    %tr
      %th id
      %th 社員番号
      %th メール
      %th 送信許可区分
      %th
  %tbody
    = render @tsushinseigyous
= render 'new'
= render 'edit'
